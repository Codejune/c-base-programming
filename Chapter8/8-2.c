/*
    다음 프로그램을 실행한 후 결과가 어떻게 나오는가? 결과가 제대로 나오지 않는다면 왜 그런지 이유를 쓰고 수정하라.

    #include <stdio.h>
    void add(int, int, int);
    int main(void){
        int x, y, result;
        printf("두 수를 입력하시오 : ");
        scanf("%d %d", &x, &y);
        add(x, y, result);
        printf("%d + %d = %d입니다.\n", x, y, result);
        return 0;
    }
    
    void add(int a, int b, int c);
        c = a + b;
    }
*/
 
